-----------------------
-------- LAYOUT -------
-----------------------

hl.config({
    dwindle = {
    preserve_split = true,
    split_width_multiplier = 2 -- lower = favors top/bottom splits more
},
})

hl.config({
    master = {
        new_status = "master",
    },
})

hl.config({
    scrolling = {
        fullscreen_on_one_column = true,
    },
})
