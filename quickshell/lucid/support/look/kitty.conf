# Lucid — kitty
# The colours live in matugen-colors.conf, rewritten on every theme change.
# Keep the include last so it wins over anything set above it.

# kitty opens fish rather than the login shell
shell fish

font_family      JetBrainsMono Nerd Font Mono
font_size 10.0

window_padding_width 25

# translucency; the frost itself comes from Hyprland's blur
background_opacity 0.7

include ./matugen-colors.conf
