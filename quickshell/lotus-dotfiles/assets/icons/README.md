# Lotus interface icons

These SVGs are project-owned line icons drawn on a 24 px view box. Keep a 2 px rounded stroke and render them at 16, 20 or 24 px. Add icons only when a working component needs them.
