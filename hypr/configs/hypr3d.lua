-- hypr3d plugin config - safe version with guard
-- All keybinds are in hyprland.conf to avoid Lua layer issues
-- This file just sets up any non-keybind config

local mainMod = "SUPER"

-- Empty - keybinds moved to hyprland.conf
